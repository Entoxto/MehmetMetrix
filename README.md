# Mehmet Metrics

## Что написано в `package.json` простыми словами

- Название проекта: `mehmet-metrics`. Пометка `private: true` просто означает, что этот проект нельзя случайно опубликовать в общий каталог npm.
- Версия сейчас `0.1.0`. Это ярлык “какая итерация”, его можно менять при релизах.
- Готовые команды:
  - `npm run dev` — запустить сайт в режиме разработки;
  - `npm run build` — собрать готовую версию;
  - `npm run start` — запустить уже собранную версию.
- Для работы сайту нужны три основные библиотеки: `next` (каркас), `react` и `react-dom` (интерфейс).
- Для разработки подключены вспомогательные пакеты: `typescript` и типы для Node/React, а также набор проверок кода (`eslint`, `eslint-config-next`, плагины `@typescript-eslint/...`). Они помогают редактору подсвечивать ошибки и держать код в порядке.

Небольшое веб-приложение для управления каталогом изделий, отслеживания партий и финансовых показателей.

## Как устроена страница карточки товара (`app/ProductCard/[id]/page.tsx`)

- **Задача файла** — показать карточку выбранного товара. Используется одна и та же страница для переходов из каталога и из списка партий.
- **Откуда берём данные** — читаем `id` товара из адресной строки и ищем его в `data/products.json`.
- **Кнопка “Назад”**:
  - если пришли из раздела “Работа”, возвращаемся туда же, раскрывая нужную партию и позицию;
  - если из каталога — открываем каталог;
  - если история браузера не пустая — просто шаг назад;
  - если история пустая — подстраховка и переход в каталог.
- **Если товар не найден** — показываем сообщение “Товар не найден” и кнопку “Назад”.
- **Главный блок**:
  - фон с градиентом и светлым текстом;
  - в шапке название товара и кнопка “Назад”; на мобильных элементы располагаются колонкой, на десктопе — в строку;
  - компонент `ProductDetail` выводит фото, размеры, цену, материалы.
- **Адаптивность** — через `useBreakpoint()` определяется ширина экрана, чтобы перестроить шапку и отступы для мобильных и планшетов.
- **Общие стили** — цвета, отступы и кнопки берём из `constants/styles.ts`.


## 📁 Структура проекта

### Корневая директория

- **`package.json`** — описание проекта, зависимости и скрипты для запуска
- **`tsconfig.json`** — настройки TypeScript (типы, пути импортов, строгость проверок)
- **`.eslintrc.json`** — правила проверки кода (ESLint)
- **`.prettierrc.json`** — правила форматирования кода (Prettier)
- **`next-env.d.ts`** — типы для Next.js (автоматически генерируется)
- **`Запустить проект.bat`** — скрипт для быстрого запуска проекта на Windows

---

## 📂 Основные папки

### `app/` — Страницы приложения (Next.js App Router)

Это основная папка для страниц в Next.js. Каждый файл здесь — это маршрут (URL).

- **`layout.tsx`** — общий шаблон для всех страниц
  - Настраивает HTML-структуру (html, body)
  - Добавляет глобальные стили
  - Определяет метаданные (title, description)
  - Настраивает viewport для мобильных устройств

- **`page.tsx`** — главная страница приложения
  - Содержит основную логику приложения
  - Управляет состоянием (какая страница открыта: меню, каталог, финансы, работа)
  - Обрабатывает данные о партиях и изделиях
  - Рендерит разные экраны в зависимости от выбранного раздела
  - Использует `<BatchView batch={batch} />` для отображения партий
  - Рефактор: логика вынесена в derive/format, компоненты унифицированы

- **`home/`** — составные части главного экрана
  - `Header.tsx`, `Footer.tsx` — шапка и подвал
  - `views/` — отдельные представления (`MenuView`, `CatalogView`, `WorkView`)
  - Эти компоненты сцепляют переиспользуемые блоки и нужны только главной странице

---

### `components/` — Переиспользуемые компоненты

Здесь лежат React-компоненты, которые можно использовать в разных местах.

#### Основные компоненты

- **`CategoryCard.tsx`** — карточка категории в меню
  - Показывает название категории, описание и количество товаров
  - При клике открывает каталог этой категории
  - Имеет hover-эффекты (подсветка при наведении)

- **`ProductCard.tsx`** — карточка товара в каталоге
  - Показывает фото, название, цену и размеры товара
  - При клике открывает детальную страницу товара

- **`ProductDetail.tsx`** — детальная страница товара
  - Показывает большое фото, все размеры, цену и материалы
  - Отображается при клике на товар в каталоге
  - Адаптивная вёрстка (разная для мобильных и десктопов)

- **`MoneyView.tsx`** — экран "Что по бабкам"
  - Показывает финансовую информацию
  - Две карточки: "Всего к оплате" и "Депозитов внесено"
  - Детализация с разбивкой по партиям и предоплатам

#### `components/ui/` — Атомарные UI-компоненты

Унифицированные компоненты для переиспользования по всему проекту.

- **`StatusBadge.tsx`** — универсальный компонент капсулы для всех статусов
  - Поддерживает разные варианты: default, info, success, warning, error
  - Адаптивный размер шрифта
  - Единый механизм отображения статусов

- **`SampleTag.tsx`** — компонент овала "образец"
  - Компактный дизайн, чуть ниже чипов размеров
  - Используется для отметки образцов

- **`SizeChips.tsx`** — чипы размеров
  - Отображает размеры в виде чипов (2 в строке, перенос, wrap/gap)
  - Адаптивная вёрстка для мобильных и десктопов

#### `components/work/` — Компоненты для работы с партиями

Компоненты для отображения партий и позиций.

- **`PositionRow.tsx`** — компонент для отображения одной позиции
  - Использует `SizeChips`, `SampleTag` и `StatusBadge`
  - При наличии `noteEnabled` → подпись под размерами через `StatusBadge`
  - Макет идентичен предыдущей версии, без дублирования логики

- **`BatchView.tsx`** — компонент для отображения партии
  - Принимает `Batch`, вызывает `toViewRows(batch)`
  - Рендерит секции по статусам в порядке `orderStatuses`
  - Для каждого статуса → `<PositionRow />`
  - Подписи статусов берутся из `format/statusLabel`

---

### `constants/` — Константы и настройки

Здесь хранятся неизменяемые данные и настройки, которые используются по всему проекту.

- **`styles.ts`** — стили и цвета
  - `COLORS` — палитра цветов (основной, успех, ошибка, фоны, границы)
  - `STYLES` — готовые стили для карточек, кнопок, бейджей
  - `SPACING` — стандартные отступы (xs, sm, md, lg, xl)
  - Один раз определили — используем везде

- **`responsive.ts`** — настройки адаптивности
  - `BREAKPOINTS` — точки перелома экранов (mobile, tablet, laptop, desktop)
  - `useBreakpoint()` — хук для определения текущего размера экрана
  - Помогает делать адаптивный дизайн

---

### `lib/` — Утилиты и вспомогательные функции

Здесь лежат функции, которые используются в разных местах проекта.

- **`utils.ts`** — вспомогательные функции
  - `formatCurrency()` — форматирует деньги (например: `$45 970` с тонким пробелом)
  - `createCardHoverHandlers()` — создаёт обработчики hover-эффекта для карточек
  - Можно добавлять другие утилиты (форматирование дат, валидация и т.д.)

- **`derive.ts`** — бизнес-логика для работы с позициями и партиями
  - `calcSum(position)` — вычисляет сумму позиции
  - `groupByStatus(positions)` — группирует позиции по статусу
  - `orderStatuses` — фиксированный порядок вывода статусов
  - `toViewRows(batch)` — преобразует партию в список строк для отображения
  - Рефактор: логика вынесена из JSX в отдельные функции

- **`format.ts`** — форматирование данных для отображения
  - `formatCurrency(n)` — форматирует валюту с тонким пробелом
  - `statusLabel` — объект с подписями для всех статусов
  - Рефактор: логика вынесена в derive/format, компоненты унифицированы

- **`adapters.ts`** — адаптеры для преобразования данных
  - `toPosition(item, products)` — преобразует сырой элемент в `Position`
  - `toBatch(config, products)` — преобразует конфигурацию партии в `Batch`
  - Преобразует данные из старого формата в новый доменный формат

---

### `types/` — TypeScript типы

Здесь определяются типы данных для TypeScript.

- **`product.ts`** — типы для товаров
  - `Product` — структура товара (id, название, цена, размеры, материалы, фото)
  - `ProductsData` — структура JSON-файла с товарами
  - Помогает TypeScript проверять правильность данных

- **`domain.ts`** — доменные типы проекта
  - `Size` — типы размеров ('XS' | 'S' | 'M' | 'L' | 'XL')
  - `PositionStatus` — enum статусов позиций (inProduction, inTransit, done, paid, paidEarlier, returned)
  - `Position` — структура позиции (id, title, sizes, qty, price, sum, sample, status, noteEnabled, noteText)
  - `Batch` — структура партии (id, receivedAt?, positions: Position[])
  - Рефактор: единые доменные типы для всего проекта

---

### `data/` — Данные приложения

Здесь хранятся статические данные.

- **`shipments.json`** — список партий, статусов и позиций (что шьётся, что пришло, что оплачено)
- **`products.json`** — база данных товаров
  - Список всех товаров с их характеристиками
  - Используется как источник данных для каталога
  - Можно редактировать вручную для добавления/изменения товаров

- **`README.md`** — описание структуры данных (если есть)

---

### `public/` — Публичные файлы

Файлы, которые доступны напрямую по URL (изображения, иконки и т.д.).

- **`images/products/`** — фотографии товаров
  - Все фото товаров хранятся здесь
  - Названия файлов соответствуют названиям товаров
  - Используются в каталоге и детальных страницах

---

## 🔧 Как это работает

1. **Запуск приложения**
   - `npm run dev` — запуск в режиме разработки
   - `npm run build` — сборка для продакшена
   - `npm start` — запуск собранной версии

2. **Основной поток**
   - Пользователь открывает сайт → видит главное меню
   - Выбирает раздел (каталог, финансы, работа) → открывается соответствующий экран
   - В каталоге можно выбрать категорию → увидеть товары этой категории
   - Клик на товар → открывается детальная страница

3. **Данные**
   - Товары загружаются из `data/products.json`
   - Партии и финансы хранятся прямо в коде (`app/page.tsx`)
   - Изображения берутся из `public/images/products/`

---

## 🎨 Технологии

- **Next.js 14** — фреймворк для React (роутинг, серверные компоненты)
- **React 18** — библиотека для создания интерфейсов
- **TypeScript** — типизированный JavaScript
- **CSS-in-JS** — стили пишутся прямо в компонентах (inline styles)

---

## 📝 Примечания

- Все стили написаны inline (внутри компонентов), без отдельных CSS-файлов
- Проект использует Next.js App Router (новая система роутинга)
- Все компоненты помечены как `"use client"` (клиентские компоненты)
- Данные о партиях и финансах хранятся в `app/page.tsx` (можно вынести в отдельные файлы)

## 🔄 Рефакторинг (2025)

Проведена внутренняя чистка проекта без изменения визуального дизайна:

### Основные изменения:

1. **Единые доменные типы** (`types/domain.ts`)
   - Определены структуры `Size`, `PositionStatus`, `Position`, `Batch`
   - Строгая типизация TypeScript везде

2. **Бизнес-логика вынесена** (`lib/derive.ts`)
   - `calcSum()` — вычисление суммы позиции
   - `groupByStatus()` — группировка по статусам
   - `toViewRows()` — преобразование партии в строки для отображения

3. **Форматирование централизовано** (`lib/format.ts`)
   - `formatCurrency()` — форматирование валюты
   - `statusLabel` — подписи для всех статусов

4. **Атомарные UI-компоненты** (`components/ui/`)
   - `StatusBadge` — универсальный компонент статуса
   - `SampleTag` — компонент "образец"
   - `SizeChips` — чипы размеров

5. **Компоненты для работы** (`components/work/`)
   - `PositionRow` — компонент позиции
   - `BatchView` — компонент партии

6. **Тесты** (`lib/derive.test.ts`)
   - Тесты для группировки и форматирования

### Результат:

- ✅ Код стал короче и чище
- ✅ Убрано дублирование логики
- ✅ Визуальный дизайн не изменён
- ✅ Строгие типы TypeScript везде
- ✅ Компоненты унифицированы

---

## 🚀 Быстрый старт

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите проект:
   ```bash
   npm run dev
   ```

3. Откройте браузер:
   ```
   http://localhost:3000
   ```

---

## 📚 Дополнительные файлы

- **`OPTIMIZATION_CHANGELOG.md`** — история оптимизаций проекта
- **`OPTIMIZATION_DIFF.txt`** — diff-файлы с изменениями
- **`lib/derive.test.ts`** — тесты для бизнес-логики
- **`.next/`** — сгенерированные файлы Next.js (не коммитить в git)
- **`node_modules/`** — установленные зависимости (не коммитить в git)

