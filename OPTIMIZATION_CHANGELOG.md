# Changelog: Оптимизация проекта

## 📋 Сводка изменений

### Удалённый код
1. **Дубликат функции `formatCurrency`** — удалены локальные объявления из 3 файлов:
   - `app/page.tsx` (строка 268-270)
   - `components/MoneyView.tsx` (строка 18-20)
   - `components/ProductDetail.tsx` (строка 9-11)

2. **Неиспользуемая функция `pickResponsiveValue`** — удалена из `constants/responsive.ts` (строка 65-70)

3. **Неиспользуемый стиль `sizeBadgeLarge`** — удалён из `constants/styles.ts` (строка 62-70)

4. **Неиспользуемый проп `cardId`** — удалён из интерфейса `CardProps` в `components/Card.tsx` (строка 10)

5. **Неиспользуемый импорт `Card`** — удалён из `components/MoneyView.tsx` (строка 4)

### Созданные файлы
1. **`lib/utils.ts`** — новая утилита для общих функций:
   - `formatCurrency` — централизованное форматирование денег

### Изменённые файлы

#### `app/layout.tsx`
- Исправлен импорт: `React.ReactNode` → `ReactNode` (type import)
- Добавлен импорт `type { ReactNode } from "react"`

#### `app/page.tsx`
- Удалено локальное объявление `formatCurrency`
- Добавлен импорт `formatCurrency` из `@/lib/utils`

#### `components/Card.tsx`
- Удалён неиспользуемый проп `cardId` из интерфейса и деструктуризации

#### `components/MoneyView.tsx`
- Удалён неиспользуемый импорт `Card`
- Удалено локальное объявление `formatCurrency`
- Добавлен импорт `formatCurrency` из `@/lib/utils`

#### `components/ProductDetail.tsx`
- Удалено локальное объявление `formatCurrency`
- Добавлен импорт `formatCurrency` из `@/lib/utils`

#### `constants/responsive.ts`
- Удалена неиспользуемая функция `pickResponsiveValue`

#### `constants/styles.ts`
- Удалён неиспользуемый стиль `sizeBadgeLarge` из объекта `STYLES`

## ✅ Проверка типов

**До оптимизации:**
- Ошибки TypeScript: 1 (конфликт импорта и локального объявления `formatCurrency`)

**После оптимизации:**
- Ошибки TypeScript: 0 ✅
- Предупреждения линтера: 0 ✅

## 📊 Статистика

- **Удалено строк кода:** ~15
- **Создано файлов:** 1 (`lib/utils.ts`)
- **Изменено файлов:** 7
- **Устранено дубликатов:** 3 функции `formatCurrency`
- **Удалено неиспользуемых элементов:** 4 (функция, стиль, проп, импорт)

## 🔍 Примечания

1. **`isTablet` и `isLaptop`** — оставлены в `useBreakpoint`, так как могут быть полезны в будущем и не влияют на производительность.

2. **Все изменения обратно совместимы** — публичные API компонентов не изменены, поведение UI сохранено.

3. **Типы улучшены** — добавлен type import для `ReactNode` в `app/layout.tsx`.

4. **Структура проекта** — создана папка `lib/` для общих утилит, что улучшает организацию кода.

## 🎯 Результат

- ✅ Устранены все дубликаты кода
- ✅ Удалён весь мёртвый код
- ✅ Улучшена структура проекта
- ✅ Все проверки TypeScript проходят успешно
- ✅ Поведение приложения не изменено

